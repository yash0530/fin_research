# Copy to .env and fill in. .env is gitignored.

# SQLite database (WAL). Local file.
DATABASE_URL="file:./data/engine.db"

# Local LLM (llama-server, Qwen 3.6 27B). Keyless.
# QWEN_BASE_URL="http://localhost:8000/v1"

# Connectivity-only cloud fallback (optional; NOT a quality crutch).
# GEMINI_API_KEY=""

# EDGAR requires a descriptive UA (enforced at startup): "Name email@example.com"
# EDGAR_USER_AGENT=""
