# scratch/ — working area

Throwaway working files and delegation artifacts. Nothing here is product code and
nothing here is imported by `src/`. Subdirs: `kiro/` holds delegation batch specs
(see its CLAUDE.md). Safe to prune old files; never reference scratch paths from
production code or docs.
