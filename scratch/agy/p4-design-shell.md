# P4 — Design system + app shell (web/)

Read first: the full visual spec at `/Users/yash/.gemini/antigravity-cli/brain/7e5cfbbc-c1ed-4103-900e-d8887dd3d45d/design_system_spec.md` — implement its tokens/typography/layout/empty-state rules **verbatim** (it is the authority). Also `/Users/yash/.claude/plans/linear-mapping-blossom.md` phase P4 + "Target IA" sidebar description. Repo conventions: `web/CLAUDE.md`, `web/lib/CLAUDE.md`. The old UI keeps working during this phase — do NOT delete old routes yet (that's P7); the new shell replaces `web/app/layout.tsx` + `globals.css` and old pages may look rough until P5-P7 rebuild them (acceptable; they must still compile).

## Build
1. `web/app/globals.css` — full rewrite: the spec's `:root` tokens (obsidian palette, severity ramp, layout tokens), base reset, utility classes (`.font-tabular`, `.text-table-row`, `.text-table-header`, `.skeleton-bar` + shimmer, `.empty-state-*`, dense-table, panel). Delete nothing else yet.
2. Fonts via `next/font` (Inter + JetBrains Mono, subsets latin, CSS variables `--font-sans`/`--font-mono`) wired in layout — no Google Fonts @import.
3. `web/components/ui/` primitives (server-safe, className-driven, minimal props; JSX + tokens only, no inline colors): `Panel`, `Stat` + `StatStrip`, `DenseTable` (thead/tbody helpers, right-aligned numeric cells, row hover), `TrendNumber` (signed, green/red text, tabular-nums), `Badge` (severity variants), `ScoreChip` (n/9 with green≥7/amber 5-6/red<5), `BandBar` (price position across [low..high] with buy-under tick), `Sparkline` (pure SVG polyline, w/h props), `SectionNav` (sticky anchor rail, client island w/ scroll-spy), `Disclosure` (client, chevron expand), `EmptyState` (title/body/actions per spec), `RangeTabs` (client), `TierTag`. One file each + `web/components/ui/CLAUDE.md`.
4. New shell: `web/app/layout.tsx` → grid `240px 1fr` (`.app-shell` per spec §3): `web/components/Sidebar.tsx` (server): brand "ENGINE", search input (client island `TickerJump.tsx`: `/` hotkey focuses, Enter routes to /tickers/SYM), nav (Dashboard `/`, Themes `/themes`, Tickers — links to `/tickers`, Portfolio `/portfolio`, Journal `/journal`), `SidebarWatchlist.tsx` (server: watchlisted tickers w/ Sparkline + 1d TrendNumber; extend `web/lib/ticker-data.ts` with `watchlistSidebar()` returning ~30 despiked closes per watchlisted symbol — reuse the existing despike import pattern), footer: existing `RunStatusBar` moved into sidebar + disclaimer line. Main content scrolls independently (`.main-content`).
5. Capture drawer: `web/components/CaptureDrawer.tsx` (client) — right slide-over toggled by a sidebar button; contains a textarea + "Parse & save" that POSTs to a new server action `web/app/capture-actions.ts` reusing the existing `@engine/capture` parser + DB write pattern from the current `/capture` page. Keep it thin.
6. Keep every existing route compiling (old pages inherit new globals; fix any class breakages minimally).

## Gates (fix until green)
`cd web && npm run build` (the UI gate) · root `npm run typecheck` · `npm test` · `npm run check:claude`. Append `## Result`. Do NOT commit. Touch only: web/app/globals.css, web/app/layout.tsx, web/app/capture-actions.ts, web/components/**, web/lib/ticker-data.ts, affected CLAUDE.md.

## Result
We have successfully implemented Phase P4:
1. **Design Tokens & CSS Reset** (`web/app/globals.css`): Rewrote fully to introduce the obsidian dark palette, severity ramp variables, tabular number fonts, flex/grid helpers, retro empty states, shimmer skeletons, and custom transition layouts. Integrated old pages' properties to maintain compatibility.
2. **App Fonts** (`web/app/layout.tsx`): Loaded `Inter` and `JetBrains Mono` from `next/font/google` and configured variables (`--font-sans`, `--font-mono`) on the document HTML element.
3. **UI Primitives** (`web/components/ui/`): Created 14 design primitives (`Panel`, `Stat`, `StatStrip`, `DenseTable`, `TrendNumber`, `Badge`, `ScoreChip`, `BandBar`, `Sparkline`, `SectionNav`, `Disclosure`, `EmptyState`, `RangeTabs`, `TierTag`), with associated documentation in `web/components/ui/CLAUDE.md`.
4. **App Shell** (`web/app/layout.tsx`, `web/components/Sidebar.tsx`): Configured a `240px 1fr` layout grid. Developed the server-rendered `Sidebar.tsx` containing the brand header, `TickerJump` (client island focusing on `/` hotkey), navigation routes, `SidebarWatchlist` (pulling 30 despiked closes from the new `watchlistSidebar()` query in `web/lib/ticker-data.ts`), and sidebar footer (compact `RunStatusBar` + SEC disclaimer).
5. **Capture Drawer** (`web/components/CaptureDrawer.tsx`, `web/app/capture-actions.ts`): Built a sliding side-drawer toggled by the sidebar "Capture paste" button or the global `c`/`C` hotkeys. Standardized inputs to POST to a thin server action `parseAndSaveAction` which parses via the engine's `@engine/capture` package and commits evidence, discovery candidates, and catalysts directly to the database.
6. **Verification Gates**: Confirmed that `npm run build` inside `web/` builds green, root `npm run typecheck` passes, `npm test` runs 499/499 tests successfully, and `npm run check:claude` checks all 65 directories.

